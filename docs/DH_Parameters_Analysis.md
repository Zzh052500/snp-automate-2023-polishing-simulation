# DH参数改变详细分析

## 什么是DH参数？

DH参数（Denavit-Hartenberg参数）是描述机械臂几何结构的4个参数组合，用来定义相邻两个关节之间的变换关系。

```
对于每个关节 i，有4个DH参数：
├─ a_i     : 连杆长度 (两个关节轴之间的距离)
├─ α_i     : 连杆扭角 (两个关节轴之间的夹角)
├─ d_i     : 关节距离 (沿关节轴方向的偏移)
└─ θ_i     : 关节角度 (可变，运动时改变)
```

这4个参数完全定义了一个关节对下一个关节的变换，累积到末端执行器后，就确定了整个机械臂的运动学特性。

---

## HC10 vs CR12A 的DH参数对比

### HC10 (安川) 的DH参数表(示例值)

| 关节 | 关节类型 | a (mm) | α (°) | d (mm) | θ范围 (°) |
|------|--------|--------|-------|--------|-----------|
| Joint1 (S轴) | 旋转 | 0 | -90° | 0 | ±170° |
| Joint 2 (L轴) | 旋转 | 150 | 0° | 0 | -150~+150° |
| Joint 3 (U轴) | 旋转 | 150 | 0° | 0 | -105~+150° |
| Joint 4 (R轴) | 旋转 | 0 | -90° | 130 | ±90° |
| Joint 5 (B轴) | 旋转 | 0 | 90° | 0 | ±130° |
| Joint 6 (T轴) | 旋转 | 0 | 0° | 80 | ±360° |

### CR12A (越疆) 的DH参数表 (示例值)

| 关节 | 关节类型 | a (mm) | α (°) | d (mm) | θ范围 (°) |
|------|--------|--------|-------|--------|-----------|
| Joint 1 | 旋转 | 0 | -90° | 0 | ±180° |
| Joint 2 | 旋转 | 165 | 0° | 0 | -150~+135° |
| Joint 3 | 旋转 | 130 | 0° | 0 | -120~+135° |
| Joint 4 | 旋转 | 0 | -90° | 115 | ±180° |
| Joint 5 | 旋转 | 0 | 90° | 0 | ±135° |
| Joint 6 | 旋转 | 0 | 0° | 75 | ±360° |

### 关键差异

```
Joint 2: a从150mm → 165mm (+15mm) ⚠️
Joint 3: a从150mm → 130mm (-20mm) ⚠️
Joint 4: d从130mm → 115mm (-15mm) ⚠️
Joint 6: d从80mm → 75mm   (-5mm)  ⚠️
```

---

## 影响链路分析

### IK求解的失败机制

```
DH参数改变
  ↓
KDL运动学链重新计算
  ↓
Forward/Inverse Kinematics完全改变
  ↓
打磨路径点无法到达
  ↓
执行失败 ❌
```

### 零位位置对比

**HC10零位：** `tool0 = (0.30m, 0m, 0.21m)`

**CR12A零位：** `tool0 = (0.295m, 0m, 0.19m)`

**偏差：** ΔX=-5mm, ΔZ=-20mm

---

## 实际问题

### 问题1：IK求解失败

打磨路径是根据HC10设计的，当替换为CR12A后：
- 部分路径点因臂长改变无法到达
- 奇异点位置改变导致某些点进入奇异区
- 关节限制范围改变导致计算的角度超限

### 问题2：奇异点位置变化

```
HC10可以正常求解的配置
  ↓
CR12A可能进入奇异点
  ↓
IK求解器无法反演
  ↓
返回无解
```

### 问题3：关节角度限制冲突

```
IK计算出: θ2 = 140°

HC10验证: ✅ 140° ∈ [-150°, 150°]
CR12A验证: ❌ 140° ∉ [-150°, 135°]
```

---

## 版本历史

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-09-17 | 浮浮酱 | 初始版本 |

---

**最后更新：** 2026-09-17
